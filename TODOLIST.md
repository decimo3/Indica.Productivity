# ToDoList

- [X] Configure `Derivation` and binding to `ContractProject`;
- [X] Move property `IsAlternative` from `FinishingPayment` to `Finishing`;
- [ ] Add `IdDerivarion` and `Derivation` navigation property on `WorkOrderService` entity;
- [ ] Add ways to define `Derivation` of services on `WorkOrderService` service;
- [ ] Update Bruno API Tester configuration files with project updates;
